# baseline_unknown

This promoted baseline never resolves a diarized speaker to a real person. It exists as the safety floor for the workspace.

It should have zero false assignments, but it will miss every known speaker.

